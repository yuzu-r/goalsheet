
<div class="container col-xs-10 col-xs-offset-1 col-sm-10 col-sm-offset-1">
  <h3> My Goals</h3>
</div>

<div class="container col-xs-10 col-xs-offset-1 col-sm-10 col-sm-offset-1">
  <table class="table table-hover">
    <tr>
      <th class="table-align-left col-sm-3">Title</th>
      <th class="table-align-left col-sm-6">Description</th>
      <th class="col-sm-3 text-center">Actions</th>
    </tr>
    <tbody>
  <% @goals.each do |g| %>

      <tr>
        <td class="table-align-left col-sm-3">
          <%= g.title %> 
        </td>
        <td class="table-align-left col-sm-6"> 
          <%= g.description %>
        </td>
        <td class="col-sm-3 text-center">
          <%= link_to '<i class="glyphicon glyphicon-pencil" aria-hidden="true"></i>'.html_safe, edit_user_goal_path(id: g.id) %>
          <%= link_to '<i class="glyphicon glyphicon-trash" aria-hidden="true"></i>'.html_safe, user_goal_path(id: g.id), method: :delete %>
        </td>
      </tr>
    </tbody>
  <% end %>
  </table>
</div>
<div class="clearfix"></div>
<div class="container col-xs-10 col-xs-offset-1 col-sm-10 col-sm-offset-1">
  <%= link_to '<i class="fa fa-plus" aria-hidden="true"></i> Add new goal'.html_safe, new_user_goal_path(current_user) %>
</div>